# /tdd-run 分解 — できるのツリーを立てる

要件を実現するための分解。作られるべきモジュールの**粒度**と**インターフェース**を決める。
成果物は `plans/<project>/test-tree.md`（確認済みのツリー）である。

## 入口条件

ルーティング（skill.md）の判定で root が**仮説**（新規型）と承認されて入る。
次のいずれかで入る。

- **新規実行** — `plans/<project>/test-tree.md` が存在しない。手順1から始める
- **新フェーズ** — `test-tree.md` が存在していても、新しいスコープ（次のユーザーストーリー等）の
  分解として入ってよい。手順1から始める。この場合、既存ツリーは前提知識として読む（既存装置の
  拡張の判断材料になる）が、編集対象ではない
- **部分木再入** — feedback 等からの構造的修正。末尾の「部分木再入口」から始める

冒頭で次を読む:

```bash
cat <plans_dir>/problem.md
cat <plans_dir>/user-story.md 2>/dev/null
```

**あわせて次の三つの規範を冒頭で読む**（リンク先の参照ではなく、実際にファイルを読む）:

- `../norms/vocabulary.md` — 辞書の読み書き作法、語彙登録ルール、不可逆性チェック
- `../norms/dialogue.md` — 提示 → 承認の作法
- `../norms/tree.md` — ツリー規範 — できるのツリーの記法・整形式、test-tree.md の台帳規約

いずれも分解の全体に及ぶ規範であり、「必要になった時点」を読む前に判断することはできない。
以降この文書が「語彙規範の**語彙登録ルール**に従って」と書くとき、そのルールの本体は規範側にある。

`problem.md` がなければ、先に `/tdd-problem` で問題を定義する。
`plans/<project>/dictionary.json` がなければ、ツリーを構築する前に `/tdd-vocab plan` を呼ぶ。

---

## できるのツリー

記法・層の意味論・コンテキスト宣言・整形式は[ツリー規範](../norms/tree.md)が定める（冒頭で読んでいる）。
この工程に固有なのは、ツリーの意味である:

- **root は問題解決の仮説**であり、利用仮説によって検証される
- **辺は導出** — 「できるためには何が必要か」という問いで、上から下へ子が析出される
- **すべての層が可変** — ツリーは理解が更新されるにつれて変わる（ツリー規範「ツリーは変わる」）

---

## 手順

### 1. 調査

既存システムを調査する。これは「最小限」で済ませる作業ではなく、**分解仮説の材料集め**として
積極的に行う。

対象は**辞書と実装の両方を並行して**調べる。辞書は改訂されながら運用されるため、
実装にのみ先例が残っていることがある。どちらか一方だけでは既存の解釈を取り逃す。

確認事項:

- **エントリーポイント** — この機能はどう到達されるか
- **既存の関連実装と、そこに現れている解釈 paradigm** — この領域を既存コードはどう解釈しているか
- **この問題は既存の配線済みモジュールの修正で解決できないか** — 新しいツリーを立てる前に問う
- **既存の型定義** — 重複と命名衝突を避けるため

辞書を調べるときは `dict-search.js` を通す（`dictionary.json` を `cat` しない —
[語彙規範](../norms/vocabulary.md)の作法）:

```bash
node "$(realpath "${CLAUDE_SKILL_DIR}")/../bin/dict-search.js" -s <keyword> [<keyword2> ...] <plans_dir>
node "$(realpath "${CLAUDE_SKILL_DIR}")/../bin/dict-search.js" -d1 <concept-name> <plans_dir>
```

### 2. root を立てる

**スコープ:** 本スキルの一回の実行は**単一のエントリーポイント**を扱う。
`problem.md` が複数のエントリーポイントを要求している場合は、実行を分ける。

`problem.md` を読み、問う: **「この問題が解決されるためには、何ができるようになっている必要があるか」**

**root は原則として役割主語のユースケースである**（[ツリー規範](../norms/tree.md)の記法と層を参照）。
装置主語の root は下記の例外に限る。以下、主語の型によって読み分ける。

**`user-story.md` がある場合:** 各シナリオの Then 句（期待される結果）を並べる。
これらの Then がすべて成り立ったときに何が言えるか — その共通分母として root の「できる」が
浮かび上がる。Then 句から逆算して root を検証し、`problem.md` から出た答えと一致するか確認する。
一致しない場合、`problem.md` とユーザーストーリーのどちらかに見落としがないか検討する。
（この逆算は役割主語 root と最も相性がよい — Then はユーザー視点の結果である。）

root が決まったら、手順1で特定したエントリーポイントとの関係を宣言する。
エントリーポイントが決定できない場合、手順1に戻って調査を完了させる。

#### 役割主語 root（原則）

主語には**登録済みの役割（`domain: "actor"`）を使う。** 未登録なら
[語彙規範](../norms/vocabulary.md)の**語彙登録ルール**（表＋承認）で actor として登録する —
**この枝での登録行為はこれだけである。**

**root について新しい概念の定義・登録は行わない。** root は概念ではなく**文**である
（辞書は語彙を持ち、ツリーが文を持つ）。

Entry point 宣言は「**この行為が系に入る場所**」として読む:

```
Entry point: <entry-point-file> → <root の行為>
```

**フェーズの束ね方の目安:** 利用仮説が能力単位で独立に検証できるなら、能力ごとにフェーズを
分けてよい。仮説同士が絡む場合は一フェーズに束ねて、行為層の中間ノードを立てる。
束ねた場合も、**利用仮説は行為層ノード単位で箇条書きにする**（feedback の粒度を保つため）。

#### 装置主語 root（例外）

**配線済み装置の能力改善**（共有中間ノードの取り出し＝セミラティスの探求）に限り、
root を装置主語にしてよい。この枝では root X は概念であり、名付けと登録の対象になる。

**まず既存の語彙で表現できないかを確認する**（[語彙規範](../norms/vocabulary.md)の
「既存語彙の確認」）。手順1の調査で**拡張可能な既存モジュールが見つかった場合、X はその既存概念とし、
新しい名前を作らない。**

新しい X を定義する場合:
「X は xxx できるもの」という形で表現する。
名前は**日本語の概念名**にする（例: `フロントマターテンプレートローダー`）。
名付けたら、[語彙規範](../norms/vocabulary.md)の**語彙登録ルール**（表＋承認）に従って提示し、
`plans/<project>/dictionary.json` の entries 配列に追加する:

`{"name":"<概念名>","en":"EnglishName","context":"<context>","domain":"application","definition":"xxx できるもの","relations":[],"src":null,"wip":{"status":"new","discovered":"tdd-run"}}`

この枝では次のように扱う:

- Entry point 宣言は**クライアント（この装置の使い手）の列挙**に置き換える
- Then 句からの逆算は行わない
- 利用仮説は「退行なし＋クライアント側の改善」の形になる
- 行為層ノードは立てない（層の整合則）

#### root の承認

**root の提案は提示するが、単独の承認は求めない。** root の切り方（フェーズの束ね方を含む）の
承認は、手順6のツリー確認が root を頂点として含めて行う。
ただし、root を立てるのに語彙の**新規登録**が必要な場合（役割の初回登録、装置主語 root の
概念登録）、その登録の表＋承認は上記のとおりその場で行う — 登録承認は語彙の承認であって、
ツリーの形の承認はあくまで手順6である。

### 3. root を赤で置く

主語の型によって読み分ける。

**装置主語 root の場合:** 「X は xxx できる」のテストを書く。
**このテストはツリーが完成するまで緑にしない。**

**役割主語 root の場合:** ロードできるモジュールが存在しないため、root のユニットテストは書けない。
代わりに、該当シナリオの**受け入れテストが赤（pending/todo の最小スケルトン）で存在すること**を確認する。
無ければここで置く。置く作業の実体は次のとおり。

`tests/acceptance/<project>*.spec.ts`（プロジェクトのランナーに合わせて `.test.ts`）に、該当ユーザーストーリーごとの `describe` と、シナリオごとの `it.todo`（または pending の `it`）を置き、実行して赤（pending）を確認する。

**シナリオの本文をここで書き込まない** — 受け入れテストの実体化は合成末尾の `/tdd-userstory run` の仕事である。
確認したら必ず手順4へ進む。**受け入れスケルトンは分解の代替ではない。**

> **後回しにしてよい場合:** ツリーとの対話でシナリオの解釈が動く見込みがあるときは、配置をツリー承認（手順6）後に後回しにしてよい。
> ただし後回しにする場合、**その旨を root 提示のメッセージに明記し、TodoWrite に積む** — 宣言なき後回しは省略と区別できない。

### 4. ツリーを分解する

「X が xxx できるためには何が必要か」を分解する。一行につき一ノード。
直接実装できる粒度の葉に到達するまで続ける。

**1ノード1能力。** 一つのノードは**単一の操作・単一の検証観点**を述べる。

**観察可能なトリガー:** 述語に「し、」「、」による動作の連結が現れたら、分割を検討する。
（例:「クリックで編集フォームに切り替わり、保存で移動を実行し、取消で戻せる」→
切り替え・実行依頼・取消の三ノードに分割する。）

分割された子が既存装置の能力を使う場合、それは新しい装置ではなく**使役関係**である
（親子＝使い手の意味論）。

未登録の役割を主語に書こうとした時点で、[語彙規範](../norms/vocabulary.md)の
**語彙登録ルール**（表＋承認）により登録する。

各ノードの命名と同時に、そのノードが属するコンテキスト（ドメイン境界）を `[context]` の形で宣言する（[ツリー規範](../norms/tree.md)のコンテキスト宣言 — 境界をまたぐ依存の再検討、共有ノードの独立ドメイン候補の判断を含む）。

分解の途中で、次の二つの契機が働く。

#### インターフェース確定の契機

**この契機は主に行為層と能力層の境界で発火する**（層の境界がインターフェースの線である）。

インターフェースの形を決めるとき、**帰結の表示**（この操作の結果がシステム利用者にどう見えるか）も同じ決定の一部である。形だけ決めて帰結の可読性を後回しにしない（後段の states 束縛チェックで捕まえるが、決定の時点で意識する）。

**一つのビューには一つの存在論。** システム利用者がコンテンツを語るビュー（ファイルツリー等）に、配管の事実（同期の帳尻、実装キーの分裂等）を混ぜない。配管の事実に知覚と操作の手段が必要な場合は、別のビュー（別の文脈）を立てることを検討する。

分解の候補が複数あるとき、それが**同じサブ概念を異なるアルゴリズムで実現しているだけ**なのか、
**異なるドメインレベルのサブ概念そのものを要求している**のかを見分ける。

**判定:** 両方の候補について分解を一段だけ描き、**子ノードの集合が変わるか**を見る。
（ソートアルゴリズムの選択は子ノードの集合を変えない。開き戸か引き戸かは変える —
蝶番とラッチが要るか、レールと戸車が要るかが変わる。）

後者の場合、**決定しないまま分解を進めない。** 決定の順序は次のとおり:

1. 既存の解釈 paradigm の確認（辞書と実装を並行して調べる）
2. `problem.md` / `user-story.md`
3. 人間への確認

決定は**構造的仮登録**として辞書に記録する。`src: null`・wip ステータスのエントリに、
**インターフェースの形と関係のみ**を定義する（意匠 — 見た目、design-token、実装ファイル — は
ここでは書かない）:

```bash
node "$(realpath "${CLAUDE_SKILL_DIR}")/../bin/dict-write.js" add --to <plans_dir>/dictionary.json --discovered tdd-run <<'EOF'
{"name":"<概念名>","en":"<ConceptName>","context":"<dir>","domain":"ui","definition":"...",
 "relations":[{"type":"references","target":"<対象概念>","note":""}],"src":null}
EOF
```

登録は[語彙規範](../norms/vocabulary.md)の**語彙登録ルール**（表＋承認）に従う。
このエントリは合成の組み込み段階で `src` が充填され、design-token が適用されて完成する。
`test-tree.md` と並ぶ、分解から合成への第二の受け渡し成果物である。

**エントリの形:**
`relations: references` は概念への操作ハンドルとしての参照に使う（例: 削除ボタンが記事を参照す
る）。`relations: contains` はハンドルの合成に使う——複数の操作ハンドルが合成されてより大きな
操作ハンドルになる関係で（例: 入力ハンドルと送信ハンドルが合成されて投稿フォームになる）、器が
中身を保持する関係ではない。空間的な配置（左右・上下）は書かない。

**登録の基準**: エントリーポイントそのもの（画面・ページ等）は、**行為が系に入る場所であり
合成の起点だから**——常に登録する。それ以外の中間の複合ハンドル（フォーム等）
は再利用可能性が基準になる。いまの文脈から切り離して他所でも使えるときに名付け、文脈固有で使い
回さないなら辞書には登録せず実装内の無名の構成のままでよい。

概念が特定の状態のときだけ意味をなす場合（例: 記事が公開中のときだけ表示される「非公開化」ボタ
ン）、その概念の辞書エントリに `states` フィールドがなければ状態の全集合を追加し、`relations[].note`
で該当状態を参照する（`ui`エントリ側に状態リストを複製しない）。

概念のどの操作を起動するかを明示したい場合（`references`だけでは「記事」を指すだけで、削除・編集
のどちらを起動するかが曖昧になる）、その概念の辞書エントリに `affordances` フィールドがなければ
操作の一覧を追加し、`relations[].note`で該当する操作を参照する（`ui`エントリ側に操作名を複製し
ない）。`affordances`はできるツリーの装置分割とは独立した、ドメイン言語としての操作一覧であり、
実際にどの装置が実現しているかは別途`src`/`@vocab`の機械チェックで追跡する（配線そのものの機械
チェックは合成の突合で行う）。

> 用語について: ここで扱っているのは**インターフェース確定**であって「UI」ではない。
> 「UI」というラベルは、UI → シグニファイア → 意匠 → 組み込み時、という連想の鎖によって
> 設計上の問題を組立工程に運んでしまう。辞書の `domain: "ui"` はツーリング上の語彙として
> そのまま使う。

#### パターン照合の契機

ツリーの形状を対象に、[構造パターン照合手続き](../norms/pattern-matching.md)を
domain=`pattern` で実行する。

### 5. 整形式チェック

ツリーを確定する前に、[ツリー規範](../norms/tree.md)の**整形式チェック**（技術語スキャン・scaffolding feasibility・loadability）を通す。
lint が存在する場合はそれを実行する。

### 6. 確認の拍 — ツリーを提示し、test-tree.md を書く

提示の前に、**行為層ノードの語彙束縛**を二方向で照合する。実行の淵（その行為ができるか）と評価の淵（その結果が読めるか）の両方が渡されて、初めてインターフェースである。

**行為ができるか（実行の淵）:** 各行為層ノードの動詞が、関与するオブジェクト概念の `affordances` / `relations` に覆われているかを見る。覆われていなければ、当該概念への `affordances` 追記を[語彙規範](../norms/vocabulary.md)の**語彙登録ルール**（表＋承認）で提案する（不可逆な操作であれば不可逆性チェックが連鎖する）。

**帰結が読めるか（評価の淵）:** 各行為層ノードについて問う — **「この行為のあと、システム利用者は何を、どのドメイン語彙で読み取るか」**。答えが関与する概念のドメイン語彙の `states`（または既存の表示関係）で表現できるなら、そのまま進む。表現できないなら、それは表示の問題ではなく**状態モデルの欠落**である。次のいずれかを行う:

1. `states` の拡張として解釈確定を行う（不可逆性チェックと同じ順序: 既存の解釈 paradigm → `problem.md` / `user-story.md` → 人間への確認）。承認は[語彙規範](../norms/vocabulary.md)の**語彙登録ルール**に従う
2. 欠落が `problem.md` の解決条件レベルに及ぶ場合、その旨を提示し、`/tdd-problem` への戻しを選択肢としてユーザーに諮る

**ツリーをユーザーに提示し、確認を得てから先へ進む。**
この確認は **root を頂点として含む** — root の切り方（フェーズの束ね方を含む）の承認もここで行う。
提示と承認の作法は[対話規範](../norms/dialogue.md)に従う（提示はそのターンの最後のテキストであること）。

**承認と同時に `plans/<project>/test-tree.md` に追記する。**
書式と台帳の規約は[ツリー規範](../norms/tree.md)に従う。
このセクションの型は**新規型**である — 見出しを `## <スコープ>（YYYY-MM-DD・新規型）` とする。
初回はファイル生成＝追記の初回。新フェーズの場合は既存ファイルの末尾に追記する。

ツリーの確定と同時に、このセッションが何を達成しようとしているのかの**利用仮説**を記録する。
利用仮説は「使ったらこうなるはず / こうなったら外れ」の形を取る — 実装の前にコミットする。
これはユーザーと共に書く。

**`user-story.md` がある場合:** 各シナリオの Given/When/Then を材料にする。
一つのシナリオが「使ったらこうなるはず」の一項目に対応する。そのシナリオが失敗する条件が
「こうなったら外れ」になる。
**`user-story.md` がない場合:** `problem.md` の「解決されたと言える状態」を材料にする。

**出口ゲート:** 役割主語 root の場合、該当シナリオの受け入れスケルトンが赤で存在することを確認する。
手順3で後回しにしていた場合は、ここで置いてから終了する。
**分解は、ツリー承認とこの確認の両方が済んで終了する。**

**ここで分解は終了する。**
合成は同一セッションで続けてもよいし、セッションを改めてもよい（ルーターが `test-tree.md` を見て合成へ導く）。
**どちらにするかをユーザーに確認する** — 「どちらでもよい」を暗黙の続行として扱わない。

---

## 部分木再入口

feedback 等からの構造的修正の入り口。**全体のやり直しはしない。**

**契機:** ルーティング時に `findings.md` が読まれている場合、戻し先が `/tdd-run` の項目のうち
ツリーに触るものが再入の対象である（findings はルーティング時に読んでいるため、ここで再読は
しない）。ユーザーが会話で直接構造的再入を指示した場合も、同様にここから入る。

1. 影響を受けるノードを**すべて含む最小の部分木**を特定して開く
2. 上の手順4〜6を、その部分木のスコープで再実行する
3. 手順6では差分を再確認し、`test-tree.md` を更新する

編集後は通常ルートで合成に合流する。
